# The "crown jewels" for the demo - obviously fake values,
# but formatted like a real prod DB credential secret so the
# leak reads convincingly on stage.
apiVersion: v1
kind: Secret
metadata:
  name: prod-db-credentials
  namespace: prod
  labels:
    demo: dandiya-defense
type: Opaque
stringData:
  DB_HOST: "prod-postgres.internal.example.com"
  DB_USER: "app_prod_user"
  DB_PASSWORD: "S3cr3t-D3m0-Only-N0t-Real!"
  DB_CONNECTION_STRING: "postgres://app_prod_user:S3cr3t-D3m0-Only-N0t-Real!@prod-postgres.internal.example.com:5432/appdb"
